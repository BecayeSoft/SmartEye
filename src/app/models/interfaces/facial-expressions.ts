/**
 * This interface serves as a dictionary where each key is the facial expression,
 * and each value is the number of individuals having that expression.
 */
export interface FacialExpressions {
    smilesCount: number
}
